package com.github.wz2cool.mybatis.dynamic.query.sqlitedemo.config;

import org.springframework.context.annotation.Configuration;

/**
 * MyBatis配置类
 */
@Configuration
public class MyBatisConfig {
    // Spring Boot会自动扫描@Mapper注解的接口，无需额外配置
}